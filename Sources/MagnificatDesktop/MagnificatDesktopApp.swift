import SwiftUI

// Placeholder entry point — replaced once AppViewModel is built test-first.
@main
struct MagnificatDesktopApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Magnificat Desktop")
        }
    }
}
